{
  "name": "rustic-enum",
  "version": "0.0.0",
  "description": "Rust's enum functionality in TypeScript",
  "type": "module",
  "module": "index.ts",
  "svelte": "index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \".\"",
    "format": "prettier --write --ignore-path=../../.prettierignore ."
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*"
  }
}
